================================================================================
 IvansTerm - TO-DO LIST (Active Pipeline)
================================================================================
범례: [ ] 미완료 | [/] 진행중 | [O] 완료 | [S] 보류 | [C] 확인완료
================================================================================

──────────────────────────────────────────────────────────────────────────────
1. 🖥️ SSH 터미널 (Terminal)
──────────────────────────────────────────────────────────────────────────────
  - [O] 1-4c. 네트워크 재연결 로직 (지수 백오프)
        > 지수 백오프 (1s→2s→4s...→30s max, 최대 10회)
        > 비정상 종료 시 자동 재연결, 의도적 종료 시 재연결 안 함
        > 재연결 시도 중 터미널에 상태 표시

──────────────────────────────────────────────────────────────────────────────
2. 📝 리모트 파일 에디터 (Editor)
──────────────────────────────────────────────────────────────────────────────
  - [C] 2-1. [백엔드] SFTP 파일 API 구현
        > GET /api/files/read — 파일 내용 읽기
        > PUT /api/files/write — 파일 저장
        > POST /api/files/mkdir — 디렉토리 생성
        > DELETE /api/files/delete — 파일/디렉토리 삭제
        > 파일 크기 제한 검증
  - [O] 2-2. [프론트] 파일 트리 (SFTP Lazy Loading)
        > FileTree 컴포넌트 — 디렉토리 클릭 시 하위 목록 로드
        > useSFTP 훅 — API 통신 로직
        > 디렉토리/파일 아이콘 구분, 정렬
  - [O] 2-3. [프론트] Monaco Editor 통합
        > 파일 클릭 → 에디터에 내용 로드
        > 파일 확장자 기반 언어 자동 감지
        > Ctrl+S 저장 단축키
        > 탭 관리 (다중 파일 열기, 닫기, dirty 표시)
        > Saving/Saved UI 피드백
  - [O] 2-4. [프론트] 에디터 실시간 파일 변경 감지
        > 서버에서 파일이 변경되면 에디터에 반영 (포커스 복귀 시 또는 주기적)
        > 변경 감지 시 사용자에게 리로드 여부 확인
  - [O] 2-5. [프론트] 에디터 멀티 뷰 (탭 / 분할)
        > 최소 2개 파일 동시 열기
        > 탭 모드: 탭으로 전환하며 보기
        > 분할 모드: 위/아래 수직 분할로 동시 보기
        > 모드 전환 버튼 제공
  - [O] 2-6. [프론트] 로그 뷰어 (좌측 하단)
        > 에디터 아래쪽에 로그 패널 (접기/펼치기)
        > 파일 선택 → tail -f 방식으로 실시간 로그 스트리밍
        > 백엔드 WebSocket 또는 폴링으로 구현

──────────────────────────────────────────────────────────────────────────────
3. 🔲 동적 패널 레이아웃 (Panel Layout)
──────────────────────────────────────────────────────────────────────────────
  - [O] 3-3. 경계선 드래그 리사이징
        > react-resizable-panels PanelResizeHandle 적용
        > 호버 시 하이라이트 + 넓은 히트 영역 (보이지 않는 확장 영역)
        > 좌우/상하 모든 패널 경계에 적용
  - [O] 3-6. 레이아웃 상태 저장 및 복구
        > GET/PUT /api/workspace — DB(workspaces 테이블)에 JSON 저장
        > 패널 크기, 뷰 모드, 열린 파일 목록, 파일 트리/로그 상태 저장
        > 앱 재시작 시 마지막 레이아웃 자동 복구 (debounce 500ms)
  - [O] 3-7. [우측] 터미널 멀티 뷰 (탭 / 분할)
        > 최대 3개 터미널
        > 탭 모드: 탭으로 전환하며 보기
        > 분할 모드: 위/아래 수직 분할 (최대 2개 동시 표시)
        > 모드 전환 버튼 제공

──────────────────────────────────────────────────────────────────────────────
4. 📂 파일 전송 (File Transfer)
──────────────────────────────────────────────────────────────────────────────

──────────────────────────────────────────────────────────────────────────────
5. 🔌 접속 및 세션 관리 (Connection & Session)
──────────────────────────────────────────────────────────────────────────────

──────────────────────────────────────────────────────────────────────────────
6. 🎨 UI 고도화 (UI/UX Enhancement)
──────────────────────────────────────────────────────────────────────────────

──────────────────────────────────────────────────────────────────────────────
7. 🚀 운영 관리 및 배포 (Deployment & Operations)
──────────────────────────────────────────────────────────────────────────────

================================================================================
📝 참고: 완료된 과거 이력은 TO-DO-ARCHIVE.md 파일에서 확인하세요.
================================================================================
